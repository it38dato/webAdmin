\# Информационный портал базовых станций (Nokia / Ericsson) 

\## 📌 Task Необходимо установить и настроить веб-сервер \*\*«Информационный портал»\*\*, в котором отображается информация о базовых станциях \*\*Nokia\*\* и \*\*Ericsson\*\*. --- ## 💡 Decision Веб-сервер изначально разрабатывается для \*\*тестовой среды Windows\*\* с использованием \*\*Django\*\* и автоматизации через \*\*BAT-скрипты\*\*. Для работы необходимо: - Python \*\*3.10\*\* - Django - Виртуальное окружение - База данных (SQLite3 / MySQL) --- ## 🛠 Установка и настройка (Windows) ### 1. Установка Python Скачать и установить \*\*Python 3.10\*\*, убедиться, что он добавлен в `PATH`. --- ### 2. Проверка Python Запустить: ```bat checkPython.bat

Скрипт проверяет наличие Python в системе.

3\. Создание Django-проекта

Запустить:

checkDjango.bat

Что делает скрипт:

• Создаёт виртуальное окружение

• Устанавливает зависимости из requirements.txt

• Создаёт Django-проект в домашней директории

• Запрашивает:

• Имя виртуального окружения (SET VENV\_NAME=...)

• Название проекта

• Логин и пароль администратора

• Показывает:

• Команду активации виртуального окружения

• Путь к исходным файлам проекта

📌 Файл requirements.txt должен находиться в той же папке, откуда запускается checkDjango.bat

4\. Запуск тестового сервера

startDjango.bat

Скрипт запрашивает:

• Название проекта

• IP-адрес

Если IP не указан, используется:

http://127.0.0.1:8000

⚠ Если в settings.py изменён параметр:

ALLOWED\_HOSTS = \[]

— необходимо указать IP и порт, прописанные в ALLOWED\_HOSTS

(например: 127.0.0.1:8000)

Остановка сервера:

CTRL + C

📂 Готовые проекты

В папке projects/ находятся готовые версии веб-сервера.

Для запуска проекта необходимо:

• Изменить параметры в settings.py:

• ALLOWED\_HOSTS

• INSTALLED\_APPS

• TEMPLATES

• DATABASES

• customColorPalette

• CKEDITOR\_5\_CONFIGS

• Скопировать и заменить файлы:

• webAdminTemplate/urls.py

• webPages/\*.html

• webApp/\*.py

• config.json

• Запустить startDjango.bat

• Ввести параметры из config.json

🗄 Работа с базами данных

SQLite3

По умолчанию используется SQLite3.

Импорт данных из Excel:

ImportExcelSqlite.py

MySQL (продакшн)

Создание БД:

CREATE DATABASE your\_database\_name CHARACTER SET utf8mb4 COLLATE utf8mb4\_unicode\_ci;

Настройка settings.py:

DATABASES = { 'default': { 'ENGINE': 'mysql.connector.django', 'NAME': 'your\_database\_name', 'USER': 'your\_user', 'PASSWORD': 'your\_password', 'HOST': 'localhost', 'PORT': '3306', 'OPTIONS': { 'init\_command': "SET sql\_mode='STRICT\_TRANS\_TABLES'", 'charset': 'utf8mb4', 'use\_unicode': True, } } }

Миграция данных SQLite → MySQL

• Экспорт данных:

importDjangoSql1.bat

• Импорт в MySQL:

importDjangoSql2.bat

🐧 Миграция проекта (Linux)

Для переноса Django-проекта с Windows на Linux используется:

migrateDjango.sh

Перед запуском:

sudo chmod +x migrateDjango.sh ./migrateDjango.sh

Проверка сервиса:

sudo systemctl status bss\_portal.service

👤 Админ-панель Django

Создание суперпользователя:

python3 manage.py createsuperuser

Для Windows можно использовать:

migrateDjango.bat

🧪 Тестовые проекты

Папка testImportKeys:

• testLibrary.py использует библиотеку importKeys.py

• Данные подтягиваются из config.txt

⚠ Для веб-портала не используется, т.к. применяется config.json и другая логика.

📎 Примечания

• Проект предназначен для тестовой и боевой среды

• Поддерживается Windows и Linux

• Основной стек: Python / Django / MySQL

